package gr.aueb.domain;

public enum ReservationStatus {
    PENDING,     // Reservation is created but not confirmed
    CONFIRMED,   // Reservation is confirmed
    CANCELLED     // Reservation is canceled
}
